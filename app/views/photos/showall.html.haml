%h1 
  Listing All Photos

%table.table.table-responsive.table-striped.table-condensed
  %thead
    %tr
      %th ID
      %th Photo
      %th Caption
      %th Album
      %th
      %th
      %th

  %tbody
    - @photos.each do |photo|
      %tr
        %td= photo.id
        %td= photo_for(photo)
        %td= photo.caption
        %td= photo.album.name
        %td= link_to 'Show', photo
        %td= link_to 'Edit', edit_photo_path(photo)
        %td= link_to 'Destroy', photo, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Photo', new_photo_path